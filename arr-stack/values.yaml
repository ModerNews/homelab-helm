# YAML anchors only - resolved at parse time, not read by any subchart.
arrDefaults:
  env: &arrEnv
    PUID: "1000"
    PGID: "1000"
    TZ: Europe/Warsaw

  resources: &arrResources
    requests:
      cpu: 25m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

  # Single mount so downloads and library share a filesystem: imports become
  # atomic renames plus hardlinks instead of copies. Do not re-split per library.
  media: &mediaVolume
    type: hostPath
    hostPath: /mnt/hdd/media
    globalMounts:
      - path: /data

  ingressAnnotations: &arrIngressAnnotations
    cert-manager.io/cluster-issuer: letsencrypt
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Servarr

sonarr:
  global:
    fullnameOverride: sonarr
    nameOverride: sonarr
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            # v2.0.0 -> v4 in one step. Back up nzbdrone.db first; if v4 refuses the
            # schema, stage through 3.0.10.1567-ls218 and retry.
            repository: lscr.io/linuxserver/sonarr
            tag: "4.0.20.3014-ls325"
            pullPolicy: Always
          env: *arrEnv
          resources: *arrResources
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 8989
  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        <<: *arrIngressAnnotations
        gethomepage.dev/name: Sonarr
        gethomepage.dev/icon: sonarr.png
        gethomepage.dev/description: Seriale
        gethomepage.dev/weight: "10"
        gethomepage.dev/widget.type: sonarr
        gethomepage.dev/widget.url: http://sonarr.arr-stack.svc.cluster.local:8989
        gethomepage.dev/widget.key: '{{ "{{HOMEPAGE_VAR_SONARR_KEY}}" }}'
        kuma.sonarr.http.name: Sonarr TV
        kuma.sonarr.http.url: https://sonarr.bay.gruzin.eu
        kuma.sonarr.http.accepted_statuscodes: '["200-299"]'
      hosts:
        - host: &sonarrHost sonarr.bay.gruzin.eu
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: sonarr-tls
          hosts:
            - *sonarrHost
  persistence:
    config:
      type: hostPath
      hostPath: /mnt/hdd/media/configs/sonarr/data
      globalMounts:
        - path: /config
    data: *mediaVolume

radarr:
  global:
    fullnameOverride: radarr
    nameOverride: radarr
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: lscr.io/linuxserver/radarr
            tag: "nightly-6.4.4.10698-ls20"
            pullPolicy: Always
          env: *arrEnv
          resources: *arrResources
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 7878
  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        <<: *arrIngressAnnotations
        gethomepage.dev/name: Radarr
        gethomepage.dev/icon: radarr.png
        gethomepage.dev/description: Filmy
        gethomepage.dev/weight: "20"
        gethomepage.dev/widget.type: radarr
        gethomepage.dev/widget.url: http://radarr.arr-stack.svc.cluster.local:7878
        gethomepage.dev/widget.key: '{{ "{{HOMEPAGE_VAR_RADARR_KEY}}" }}'
        kuma.radarr.http.name: Radarr Movies
        kuma.radarr.http.url: https://radarr.bay.gruzin.eu
        kuma.radarr.http.accepted_statuscodes: '["200-299"]'
      hosts:
        - host: &radarrHost radarr.bay.gruzin.eu
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: radarr-tls
          hosts:
            - *radarrHost
  persistence:
    config:
      type: hostPath
      hostPath: /mnt/hdd/media/configs/radarr/config
      globalMounts:
        - path: /config
    data: *mediaVolume

prowlarr:
  global:
    fullnameOverride: prowlarr
    nameOverride: prowlarr
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: lscr.io/linuxserver/prowlarr
            tag: "nightly-2.6.5.5620-ls16"
            pullPolicy: Always
          env: *arrEnv
          resources: *arrResources
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 9696
  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        <<: *arrIngressAnnotations
        gethomepage.dev/name: Prowlarr
        gethomepage.dev/icon: prowlarr.png
        gethomepage.dev/description: Indeksery
        gethomepage.dev/weight: "30"
        gethomepage.dev/widget.type: prowlarr
        gethomepage.dev/widget.url: http://prowlarr.arr-stack.svc.cluster.local:9696
        gethomepage.dev/widget.key: '{{ "{{HOMEPAGE_VAR_PROWLARR_KEY}}" }}'
        kuma.prowlarr.http.name: Prowlarr
        kuma.prowlarr.http.url: https://prowlarr.bay.gruzin.eu
        kuma.prowlarr.http.accepted_statuscodes: '["200-299"]'
      hosts:
        - host: &prowlarrHost prowlarr.bay.gruzin.eu
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: prowlarr-tls
          hosts:
            - *prowlarrHost
  persistence:
    config:
      type: hostPath
      hostPath: /mnt/hdd/media/configs/prowlarr/config
      globalMounts:
        - path: /config

bazarr:
  global:
    fullnameOverride: bazarr
    nameOverride: bazarr
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: lscr.io/linuxserver/bazarr
            tag: "v1.6.1-ls364"
            pullPolicy: Always
          env: *arrEnv
          resources: *arrResources
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 6767
  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        <<: *arrIngressAnnotations
        gethomepage.dev/name: Bazarr
        gethomepage.dev/icon: bazarr.png
        gethomepage.dev/description: Napisy
        gethomepage.dev/weight: "40"
        gethomepage.dev/widget.type: bazarr
        gethomepage.dev/widget.url: http://bazarr.arr-stack.svc.cluster.local:6767
        gethomepage.dev/widget.key: '{{ "{{HOMEPAGE_VAR_BAZARR_KEY}}" }}'
        kuma.bazarr.http.name: Bazarr
        kuma.bazarr.http.url: https://bazarr.bay.gruzin.eu
        kuma.bazarr.http.accepted_statuscodes: '["200-299"]'
      hosts:
        - host: &bazarrHost bazarr.bay.gruzin.eu
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: bazarr-tls
          hosts:
            - *bazarrHost
  persistence:
    config:
      type: hostPath
      hostPath: /mnt/hdd/media/configs/bazarr/config
      globalMounts:
        - path: /config
    data: *mediaVolume

qbittorrent:
  global:
    fullnameOverride: qbittorrent
    nameOverride: qbittorrent
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: lscr.io/linuxserver/qbittorrent
            # 4.3.9 -> 5.2.3 also crosses libtorrent 1.x -> 2.x, so resume data is
            # rewritten on first start and torrents may recheck.
            tag: "5.2.3_v2.0.14-ls477"
            pullPolicy: Always
          env:
            <<: *arrEnv
            WEBUI_PORT: "8080"
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 8080
      # Without this the Service becomes qbittorrent-main once the controller
      # owns several, breaking every in-cluster reference.
      forceRename: qbittorrent
    tcp:
      controller: main
      type: LoadBalancer
      loadBalancerIP: 192.168.0.242
      ports:
        bittorrent:
          port: 6881
          protocol: TCP
    udp:
      controller: main
      type: LoadBalancer
      loadBalancerIP: 192.168.0.241
      ports:
        bittorrent:
          port: 6881
          protocol: UDP
  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
        gethomepage.dev/enabled: "true"
        gethomepage.dev/group: Servarr
        gethomepage.dev/name: qBittorrent
        gethomepage.dev/icon: qbittorrent.png
        gethomepage.dev/description: Torrenty
        gethomepage.dev/weight: "50"
        gethomepage.dev/widget.type: qbittorrent
        gethomepage.dev/widget.url: http://qbittorrent.arr-stack.svc.cluster.local:8080
        gethomepage.dev/widget.username: '{{ "{{HOMEPAGE_VAR_QBITTORRENT_USERNAME}}" }}'
        gethomepage.dev/widget.password: '{{ "{{HOMEPAGE_VAR_QBITTORRENT_PASSWORD}}" }}'
        kuma.qbittorrent.http.name: qBittorrent
        kuma.qbittorrent.http.url: https://qbittorrent.gruzin.eu
        kuma.qbittorrent.http.accepted_statuscodes: '["200-299"]'
      hosts:
        - host: &qbHost qbittorrent.gruzin.eu
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: qbittorrent-tls
          hosts:
            - *qbHost
  persistence:
    config:
      type: hostPath
      hostPath: /mnt/hdd/opt/qbittorrent
      globalMounts:
        - path: /config
    data: *mediaVolume
    # Temporary alias: existing torrents record /media/downloads and would go
    # missing without it. Drop once they are re-pathed onto /data.
    legacy:
      type: hostPath
      hostPath: /mnt/hdd/media
      globalMounts:
        - path: /media

jellyseerr:
  global:
    fullnameOverride: jellyseerr
    nameOverride: jellyseerr
  controllers:
    main:
      type: deployment
      strategy: Recreate
      containers:
        main:
          image:
            repository: ghcr.io/seerr-team/seerr
            tag: "v3.4.1"
            pullPolicy: Always
          env:
            TZ: Europe/Warsaw
            LOG_LEVEL: info
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            # Raised from 512Mi, which the pod had accumulated 1271 restarts against.
            limits:
              cpu: 500m
              memory: 1Gi
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 5055
  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
        gethomepage.dev/enabled: "true"
        gethomepage.dev/group: Servarr
        gethomepage.dev/name: Jellyseerr
        gethomepage.dev/icon: jellyseerr.png
        gethomepage.dev/description: Zamowienia
        gethomepage.dev/weight: "5"
        gethomepage.dev/widget.type: jellyseerr
        gethomepage.dev/widget.url: http://jellyseerr.arr-stack.svc.cluster.local:5055
        gethomepage.dev/widget.key: '{{ "{{HOMEPAGE_VAR_JELLYSEERR_KEY}}" }}'
        kuma.jellyseerr.http.name: Jellyseerr
        kuma.jellyseerr.http.url: https://jellyseerr.gruzin.eu
        kuma.jellyseerr.http.accepted_statuscodes: '["200-299"]'
      hosts:
        - host: &seerrHost jellyseerr.gruzin.eu
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: jellyseerr-tls
          hosts:
            - *seerrHost
  persistence:
    config:
      type: hostPath
      hostPath: /mnt/hdd/opt/jellyseerrconfig
      globalMounts:
        - path: /app/config

# Namespace-level Vault wiring for this chart's non-OIDC secrets. `name` is what
# recyclarr's VaultStaticSecrets resolve vaultAuthRef against - VaultAuth is
# namespaced, so without this pair existing in arr-stack the secrets fail with
# `VaultAuth "arr-stack" not found` and the sync-wave -1 gate blocks the release.
#
# `role` is a dedicated Vault Kubernetes auth role bound to arr-stack/default and
# carrying the recyclarr-read policy. It is deliberately NOT `vso`: despite the
# name, vso is the OIDC reader, bound across six namespaces, and its policy grants
# only oidc/* and keycloak/admin - it cannot read these paths.
vault:
  enabled: true
  name: arr-stack
  address: http://transit-vault.transit-vault.svc.cluster.local:8200
  role: recyclarr
  serviceAccount: default

# Converge-and-exit tool, so it runs as a CronJob. Keys come from Vault and are
# referenced with !env_var, which needs each instance's API key pre-seeded.
recyclarr:
  enabled: true
  image:
    repository: ghcr.io/recyclarr/recyclarr
    tag: "8.7.2"
  schedule: "0 4 * * *"
  vault:
    mount: secret
    refreshAfter: 1h
    keys:
      SONARR_MAIN_API_KEY: sonarr/api-key
      RADARR_MAIN_API_KEY: radarr/api-key
  # Bodies below are the TRaSH Guides root templates, inlined. Recyclarr's
  # `include:` mechanism is NOT used: as of 8.x the official config-templates
  # provider ships zero includes (`config list templates --includes` returns 0),
  # so the old v5-era include names (sonarr-v4-custom-formats-web-1080p and
  # friends) resolve against nothing and abort the whole sync. Regenerate a body
  # with `recyclarr config create -t <template>` and paste it here.
  #
  #   sonarr.series <- web-1080p AND sonarr-anime-remux-1080p (one instance)
  #   radarr.movies <- hd-bluray-web
  #
  # Instance names must be unique ACROSS services, not just within one: the CLI's
  # `--instance` flag takes a bare name with no service qualifier, so a `main` under
  # both sonarr and radarr collides and recyclarr SILENTLY drops both
  # (`[DBG] Duplicate instances: ["main"]`). Hence series/movies, not main/main.
  config:
    sonarr:
      # One Sonarr serves both anime and non-anime. Sonarr differentiates per
      # series (Series Type, quality profile, root folder), and Jellyseerr drives
      # that split through its per-server anime fields.
      #
      # `quality_definition` is the one anime-relevant setting that is NOT
      # per-series - it is a single instance-wide MB/min table. `anime` is chosen
      # deliberately: its floor is 5 MB/min against `series`' 15, and anime
      # compresses well enough that a good 1080p WEB release often lands at 6-8.
      # Under the `series` table those get rejected as too small. The cost is that
      # live action loses its size floor, so weak encodes are caught by custom
      # format scoring instead. Do not "fix" this back to `series` without moving
      # anime to its own instance.
      series:
        base_url: http://sonarr.arr-stack.svc.cluster.local:8989
        api_key_env: SONARR_MAIN_API_KEY
        quality_definition:
          type: anime
        quality_profiles:
          - trash_id: 72dae194fc92bf828f32cde7744e51a1   # WEB-1080p
            reset_unmatched_scores:
              enabled: true
          # Remux-first in the guide (~10-15GB/episode). The array runs ~78% full,
          # so the ceiling is pulled down to WEB 1080p; one remux season would eat
          # most of the remaining headroom. Verified applied: the synced profile
          # came back with cutoff=WEB 1080p and Remux absent from the allowed set.
          - trash_id: 20e0fc959f1f1704bed501f23bdae76f   # [Anime] Remux-1080p
            upgrade:
              allowed: true
              until_quality: WEB 1080p
              until_score: 10000
            reset_unmatched_scores:
              enabled: true
        # Scoped with assign_scores_to. Without it these groups apply to EVERY
        # profile in the instance, which would push live-action scoring onto the
        # anime profile. The anime profile carries no groups of its own: upstream
        # ships an empty `add:` and leans on required CFs, which sync regardless.
        custom_format_groups:
          add:
            - trash_id: 158188097a58d7687dee647e04af0da3  # [Optional] Golden Rule HD
              assign_scores_to:
                - trash_id: 72dae194fc92bf828f32cde7744e51a1
            - trash_id: 74aff4168620ed49dcc67e92b2c2a5b4  # [Optional] Language Profiles
              assign_scores_to:
                - trash_id: 72dae194fc92bf828f32cde7744e51a1
            - trash_id: 85fae4a2294965b75710ef2989c850eb  # [Streaming Services] HD/UHD boost
              assign_scores_to:
                - trash_id: 72dae194fc92bf828f32cde7744e51a1
            - trash_id: 59c3af66780d08332fdc64e68297098f  # [Unwanted] Unwanted Formats
              assign_scores_to:
                - trash_id: 72dae194fc92bf828f32cde7744e51a1

    radarr:
      # `quality_definition` is instance-global: it rewrites size limits for every
      # profile on this Radarr, not just the one below.
      movies:
        base_url: http://radarr.arr-stack.svc.cluster.local:7878
        api_key_env: RADARR_MAIN_API_KEY
        quality_definition:
          type: movie
        quality_profiles:
          - trash_id: d1d67249d3890e49bc12e275d989a7e9   # HD Bluray + WEB
            reset_unmatched_scores:
              enabled: true
        custom_format_groups:
          add:
            - trash_id: f8bf8eab4617f12dfdbd16303d8da245  # [Optional] Golden Rule HD
            - trash_id: a3ac6af01d78e4f21fcb75f601ac96df  # [Unwanted] Unwanted Formats

jellyseerrOidc:
  enabled: true
  clientId: jellyseerr
  displayName: Jellyseerr
  keycloakCRName: keycloak
  # Rendered into the keycloak namespace: the operator resolves keycloakCRName
  # only within the client's own namespace.
  keycloakNamespace: keycloak
  realm: homelab
  # Credential source. VSO reads this KV entry and writes Secret/oidc-jellyseerr into
  # this namespace; the entry holds a single `client_secret` key.
  vault:
    address: http://transit-vault.transit-vault.svc.cluster.local:8200
    mount: secret
    path: oidc/jellyseerr
    refreshAfter: 1h
  redirectUris:
    - https://jellyseerr.gruzin.eu/login?provider=keycloak&callback=true

jellyfin:
  image:
    pullPolicy: Always

  deploymentStrategy:
    type: Recreate

  securityContext:
    runAsUser: 1000
    runAsGroup: 1000

  # uid 1000 needs the host render group to open /dev/dri for VAAPI.
  # gid 105 is render on this Fedora node; verify with getent group render.
  podSecurityContext:
    supplementalGroups: [105]

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "2"
      memory: 2Gi

  persistence:
    config:
      enabled: true
      existingClaim: jellyfin-config
    media:
      enabled: false
    cache:
      enabled: true
      type: hostPath
      hostPath: /mnt/hdd/media/configs/jellyfin/cache

  ingress:
    enabled: true
    className: traefik
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt
      gethomepage.dev/enabled: "true"
      gethomepage.dev/name: Jellyfin
      gethomepage.dev/group: Streaming
      gethomepage.dev/icon: jellyfin.png
      gethomepage.dev/pod-selector: app.kubernetes.io/name=jellyfin
      gethomepage.dev/weight: "10"
      gethomepage.dev/widget.type: jellyfin
      gethomepage.dev/widget.url: http://arr-stack-jellyfin.arr-stack.svc.cluster.local:8096
      gethomepage.dev/widget.key: "{{HOMEPAGE_VAR_JELLYFIN_KEY}}"
      gethomepage.dev/widget.version: "2"
      gethomepage.dev/widget.enableNowPlaying: "true"
      gethomepage.dev/widget.expandOneStreamToTwoRows: "false"
    hosts:
      - host: jellyfin.gruzin.eu
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: jellyfin-tls
        hosts:
          - jellyfin.gruzin.eu

  volumes:
    - name: tv
      hostPath:
        path: /mnt/hdd/media/contents/tvseries
    - name: movies
      hostPath:
        path: /mnt/hdd/media/contents/movies
    - name: anime
      hostPath:
        path: /mnt/hdd/media/contents/anime
    - name: fonts
      hostPath:
        path: /mnt/hdd/media/configs/jellyfin/fonts
    # Single-node cluster, so a hostPath device is safe. Transcoding also needs
    # HardwareAccelerationType set in encoding.xml via the Jellyfin UI.
    - name: dri
      hostPath:
        path: /dev/dri

  volumeMounts:
    - name: tv
      mountPath: /tv
    - name: movies
      mountPath: /movies
    - name: anime
      mountPath: /anime
    - name: fonts
      mountPath: /usr/local/share/fonts/custom
      readOnly: true
    - name: dri
      mountPath: /dev/dri

# Two path conventions coexist here on purpose, mid-migration.
#
#   <app>/api-key    owner-scoped: the leaf names the secret, so one path can have
#                    several readers. sonarr/sonarr-anime/radarr are read by BOTH
#                    homepage and recyclarr - storing the key twice would mean a
#                    rotation has to land in two places or silently half-break.
#   <app>/homepage   the original consumer-scoped layout, still used by the apps
#                    that have exactly one reader and so no reason to move yet.
#
# Vault's homepage-read policy globs both (`secret/data/+/homepage` and
# `secret/data/+/api-key`), so the mix is safe. Move an app to api-key whenever a
# second consumer appears; migrate the rest when convenient.
homepageSecrets:
  enabled: true
  namespace: homepage
  vaultAuthRef: homepage
  mount: secret
  refreshAfter: 1h
  apps:
    - name: homepage-jellyfin
      path: jellyfin/homepage
      env:
        HOMEPAGE_VAR_JELLYFIN_KEY: api_key
    - name: homepage-sonarr
      path: sonarr/api-key
      env:
        HOMEPAGE_VAR_SONARR_KEY: api_key
    - name: homepage-radarr
      path: radarr/api-key
      env:
        HOMEPAGE_VAR_RADARR_KEY: api_key
    - name: homepage-bazarr
      path: bazarr/api-key
      env:
        HOMEPAGE_VAR_BAZARR_KEY: api_key
    - name: homepage-prowlarr
      path: prowlarr/homepage
      env:
        HOMEPAGE_VAR_PROWLARR_KEY: api_key
    - name: homepage-jellyseerr
      path: jellyseerr/homepage
      env:
        HOMEPAGE_VAR_JELLYSEERR_KEY: api_key
    - name: homepage-qbittorrent
      path: qbittorrent/homepage
      env:
        HOMEPAGE_VAR_QBITTORRENT_USERNAME: username
        HOMEPAGE_VAR_QBITTORRENT_PASSWORD: password
